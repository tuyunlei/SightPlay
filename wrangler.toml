name = "sightplay"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist"

# Create a KV namespace first, then replace this placeholder ID.
# Example: wrangler kv namespace create AUTH_STORE
[[kv_namespaces]]
binding = "AUTH_STORE"
id = "placeholder-will-be-set-after-kv-creation"

# Configure runtime secrets (not in source control):
# wrangler secret put JWT_SECRET
# wrangler secret put GEMINI_API_KEY
